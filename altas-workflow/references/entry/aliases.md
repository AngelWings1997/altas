# ALTAS Entry Aliases

> 本文件是 ALTAS Workflow 入口别名的单一维护源。
> `SKILL.md`、`README.md`、`QUICKSTART.md`、`references/spec-driven-development/workflow-quickref.md` 中涉及触发词或别名时，均应以本文件为准。

## 使用约定

- `主触发词`：用户文档中优先展示的标准写法。
- `支持别名`：与主触发词等价、可用于唤起相同模式或动作的写法。
- `模式内控制词`：仅在进入对应模式后使用，不应与全局入口触发词混淆。

## 触发词格式说明

ALTAS Workflow 的触发词采用三种格式，各有其用途：

| 格式类型 | 示例 | 用途说明 |
|----------|------|----------|
| **符号型** | `>>` | 极速通道，符号直观表示"快速执行" |
| **大写型** | `DEBUG`、`DEEP`、`TEST` | 模式切换，大写醒目便于识别 |
| **命令型** | `sdd_bootstrap`、`REVIEW SPEC` | 复合动作，包含动作和参数 |

**格式选择原则**：
1. 单字母符号（`>>`）保留给最高频的极速通道
2. 单词大写（`DEBUG`）用于模式切换，便于快速识别
3. 下划线或空格分隔（`sdd_bootstrap`、`REVIEW SPEC`）用于复合动作
4. 中文别名与英文主触发词等价，便于中文用户使用

**冒号使用规则**：
- 触发词后加冒号（如 `DEBUG:`、`TEST:`、`sdd_bootstrap:`）是**可选的**，用于分隔触发词和任务描述
- `>>` 后通常不加冒号（直接写 `>> 改配置`），加冒号也可以（`>>: 改配置`）
- 所有触发词加不加冒号均等价，冒号仅为可读性分隔符

## 全局触发词词典

| 模式 / 动作 | 主触发词 | 支持别名 | 说明 |
|-------------|----------|----------|------|
| 极速通道 | `>>` | `FAST`、`快速` | `>>` 适合极小改动；`FAST` 是同一路由的显式写法 |
| 标准启动 | `sdd_bootstrap` | 无 | RIPER 标准启动动作 |
| 深度模式 | `DEEP` | 无 | 默认按 `L` 规模处理 |
| 调试模式 | `DEBUG` | `排查`、`日志分析`、`验证功能` | `验证功能` 仍归入 DEBUG 路由 |
| 文档模式 | `DOC` | `写文档` | 文档撰写与结构化整理 |
| 功能级地图 | `MAP` | `链路梳理`、`只看代码` | 输出功能级 CodeMap |
| 项目级地图 | `PROJECT MAP` | `MAP ALL`、`全局地图`、`项目总图` | 输出项目级 CodeMap |
| 归档模式 | `ARCHIVE` | `归档`、`沉淀` | 生成 human / llm 双视角沉淀 |
| 审查模式 | `REVIEW` | `代码审查`、`审查 PR` | 通用审查入口 |
| 计划评审 | `REVIEW SPEC` | `评审规格`、`计划评审` | 执行前审查 Spec / Plan |
| 执行复盘 | `REVIEW EXECUTE` | `代码评审`、`实现复盘` | 执行后三轴评审 |
| 重构模式 | `REFACTOR` | `重构` | 重构与结构调整 |
| 测试模式 | `TEST` | `写测试`、`补测试` | 测试补齐与测试治理 |
| 性能模式 | `PERF` | `性能优化` | 性能基线、定位与优化 |
| 迁移模式 | `MIGRATE` | `迁移`、`版本升级` | 版本、框架、数据或流程迁移 |
| PRD 分析 | `PRD` | `PRD ANALYSIS`、`需求分析`、`评审 PRD`、`PRD 质量` | PRD 文档分析、验证与质量提升 |
| 多项目模式 | `MULTI` | `多项目` | 自动发现子项目并建立作用域 |
| 跨项目改动 | `CROSS` | `跨项目` | 显式允许当前轮跨项目改动 |
| 退出协议 | `EXIT ALTAS` | `退出协议` | 输出摘要与恢复锚点后退出 |

## 模式内控制词

### REVIEW 三触发词对比

三个 REVIEW 相关触发词语义不同，避免混淆：

| 触发词 | 阶段 | 输入 | 输出 |
|--------|------|------|------|
| `REVIEW` | 独立审查 | 任意代码/实现 | 三轴评审报告 |
| `REVIEW SPEC` | 执行前 | Spec/Plan 文档 | 规格质量评估 |
| `REVIEW EXECUTE` | 执行后 | 已完成的实现 | 实现三轴评审 |

### MULTI / CROSS

| 控制意图 | 主控制词 | 支持别名 | 说明 |
|----------|----------|----------|------|
| 切换活跃项目 | `SWITCH <project_id>` | `切换 <project_id>` | 切换 `active_project` 并加载目标 codemap |
| 查看项目注册表 | `REGISTRY` | `项目列表` | 展示当前 Project Registry |
| 回到本地作用域 | `SCOPE LOCAL` | `回到本地` | 重置 `change_scope=local` |

## 维护规则

- **同步义务**（强制）：`trigger_keywords` 的同步是强制的。修改本文件后**必须**同步更新 `SKILL.md` frontmatter `trigger_keywords`，遗漏视为违规。
- 新增或删除入口别名时，先更新本文件，再同步 `SKILL.md` frontmatter `trigger_keywords`。
- 其他入口文档优先引用本文件，不再各自维护完整别名清单。
- 若某个词只在特定模式内有效，应放在"模式内控制词"，不要直接放入"全局触发词词典"。
- 同步验证：运行 `scripts/validate_aliases_sync.py` 确保本文件与 `SKILL.md` frontmatter `trigger_keywords` 完全一致。
